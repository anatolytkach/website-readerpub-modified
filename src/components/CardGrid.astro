---
interface Props {
	columns?: "2" | "3" | "4";
	class?: string;
	[key: string]: unknown;
}

const { columns = "3", class: className = "", ...attrs } = Astro.props;
---

<div {...attrs} class:list={["card-grid", `card-grid--${columns}`, className]}>
	<slot />
</div>
