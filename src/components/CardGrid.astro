---
interface Props {
	columns?: "2" | "3" | "4";
	class?: string;
}

const { columns = "3", class: className = "" } = Astro.props;
---

<div class:list={["card-grid", `card-grid--${columns}`, className]}>
	<slot />
</div>
